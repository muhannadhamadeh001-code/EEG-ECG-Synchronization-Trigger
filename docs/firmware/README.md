## Firmware

Firmware will implement a simple one-shot digital trigger.

Expected behavior:
- Device waits idle after power-up
- Trigger is generated via USB serial command or user input
- One digital pulse is produced
- No repeated triggers unless reset

Firmware will be added after hardware validation.
